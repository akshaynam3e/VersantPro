# Memory

- Initialized as a React 19 + Vite WebDev static project.
- Babylon.js and Firebase client SDK are installed.
- Existing VersantPro static HTML files live separately under `/home/ubuntu/upload`; this project is an integration-ready game rather than a destructive replacement.
- Visual direction: dark navy arena, cyan player energy, hot-magenta rival energy, hard-edged sci-fi panels, large reaction prompt.
- `?demo` is the deterministic verification path.
- Before deployment, enable Firebase Anonymous Authentication and Realtime Database for the supplied Firebase project, then review the included database rules.
